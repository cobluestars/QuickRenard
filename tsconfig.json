{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig to read more about this file */

    /* Language and Environment */
    "target": "es2016",                                  /* 출력되는 JavaScript의 ECMAScript 버전을 지정 */
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],                                                   /* 컴파일에 포함될 라이브러리 선언 파일을 지정 */
    "jsx": "react",                                      /* React와 함께 JSX를 사용할 때 생성되는 JSX 코드 형식을 지정 */

    /* Modules */
    "module": "commonjs",                                /* 생성되는 모듈 코드의 형식을 지정. Node.js 환경과 호환. */
    "rootDir": "src",                                    /* 소스 파일이 있는 루트 디렉토리를 지정. */

    /* JavaScript Support */
    "allowJs": true,                                     /* 프로젝트에 JavaScript 파일 포함을 허용. */

    /* Emit */
    "outDir": "dist",                                    /* 모든 출력 파일을 위한 출력 폴더를 지정. */

    /* Interop Constraints */
    "allowSyntheticDefaultImports": true,                /* 기본 export가 없는 모듈에서 'import x from y' 형식을 허용. */
    "esModuleInterop": true,                             /* CommonJS 모듈을 쉽게 가져올 수 있도록 추가 JavaScript를 출력. */
    "forceConsistentCasingInFileNames": true,            /* import에서 파일 이름의 대소문자 일관성을 강제. */

    /* Type Checking */
    "strict": true,                                      /* 모든 엄격한 타입 검사 옵션을 활성화. */
    "noFallthroughCasesInSwitch": true,                  /* switch문에서 case 사이의 실행 흐름을 방지. */

    /* Completeness */
    "skipLibCheck": true                                 /* 모든 .d.ts 파일의 타입 검사를 생략. */
  }
}
